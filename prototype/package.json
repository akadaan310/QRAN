{
  "name": "interstellar-quran-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "The Interstellar Quran explorer — the whole corpus, the lexicon by usage, generative journeys, and an interface whose only words are the Quran's own.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "fixtures": "node scripts/gen-fixtures.mjs",
    "verify": "node scripts/verify.mjs",
    "walk": "node scripts/walk.mjs"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "playwright": "^1.63.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
