# Radial Spatial Dissolution

[Cross_Plane_Layer]: Cross-Architecture
[Core_Pillar]: [Pillar: قرآن]
[Unified_Dependency]: R2→Ledger Pipeline; R1↔R2; R2↔R3

[Runway]: Separate parallel threads stop being measured apart and are absorbed into one parent value the moment a boundary condition is met. What were many quantities becomes a single localized reading. The network synchronizes by dissolving its own divisions.

[Excavator]:
- `repo2-isnaad/src/lib/isnad.ts` — the continuity product: lexical continuity and viewpoint motion, two separately measurable quantities, merge into one master score at the seam (704 shipped instances).
- `repo1-mirtal/ops.py` — the union operator: separate clusters are absorbed into one parent cluster only under attestation; unattested unions are refused.
- `repo3-arabic/lib/engine/collapse.ts` — the collapse pipeline: a candidate set is absorbed through filters into a single surviving reading, with skipped candidates reported.
- Ledger rows emitted: 704 (full run, no cap).

[Collider]:

| Aspect | al-Mirtāl (R1) | Isnād Studio (R2) | ARABIC_TIMELESS (R3) |
|---|---|---|---|
| Detection surface | Cluster unions gated by attestation | Two measures merging into one product at a seam | Candidate sets absorbed through declared filters |
| Structural behavior | Merge only under license; otherwise the split is preserved | Merge as scoring: the product replaces its factors | Merge as filtering: survivors replace the set |
| Limit / friction | Every architecture merges, but each demands a different license — attestation, score, declared filter. Dissolution is never free; the price is where the architectures disagree |
