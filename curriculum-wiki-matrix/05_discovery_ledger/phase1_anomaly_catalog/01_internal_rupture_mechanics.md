# Internal Rupture Mechanics

[Cross_Plane_Layer]: Cross-Architecture
[Core_Pillar]: [Pillar: قرآناً عربياً]
[Unified_Dependency]: R2→Ledger Pipeline; R2↔R3

[Runway]: A single unified block of text suddenly speaks from inside itself, opening an inner voice that no outside signal summoned. The surrounding material keeps its shape and memory intact while the new voice carries its own local rules. What was one flat record is now two nested chambers sharing one wall.

[Excavator]:
- `repo2-isnaad/src/lib/engine/detectors.ts` — the absence-to-address detector: scans a region bound to absence, then flags the exact word where direct address ignites (1,446 shipped instances); the nested-speech detector: flags speech opening inside speech, carrying its own frame (12 shipped instances).
- `repo2-isnaad/data/index/discoveries.json` — the shipped finding set carrying seam positions and composite scores for every rupture instance.
- Ledger rows emitted: 1,458 (full run, no cap).

[Collider]:

| Aspect | al-Mirtāl (R1) | Isnād Studio (R2) | ARABIC_TIMELESS (R3) |
|---|---|---|---|
| Detection surface | Weighted typed edges between recitation positions; no inner-voice detector — ruptures surface only as high-weight adjacency anomalies | Dedicated detectors with per-kind evidence: absence length, address distance, speaker anchor | No voice model at all — the skeleton denotes a set of words, so a rupture is invisible by construction |
| Structural behavior | Flat graph; ruptures are edge-weight spikes | Nested loops with local properties inside a stable parent allocation | Flat strings; nesting depth is not a concept |
| Limit / friction | R1 cannot name the voice, only its weight | R2 prices the seam but cannot say what the voice means | R3 cannot see the phenomenon — the contract's flatness is the friction |
