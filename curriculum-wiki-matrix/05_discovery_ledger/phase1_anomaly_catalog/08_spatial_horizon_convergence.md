# Spatial Horizon Convergence

[Cross_Plane_Layer]: Cross-Architecture
[Core_Pillar]: [Pillar: قرآن]
[Unified_Dependency]: R1↔R2↔R3; R2→Ledger Pipeline

[Runway]: Separate architectures running in different eras arrive at exactly the same footprint without ever meeting. Their internal geometries match so precisely that expiration dates stop applying. Convergence this exact is not coincidence; it is shared structure surfacing twice.

[Excavator]:
- `repo3-arabic/spec/20-invariance.md` — timelessness: methods must be executable in any era, with hand, surface, and memory alone; era-independence as a design constraint.
- `repo2-isnaad/scripts/verify-exemplars.ts` — the exemplar verifier: 8 messenger-contour echoes confirmed against the shipped finding set, each one a footprint matched across distant regions of the array.
- `repo1-mirtal/edges_table.py` — the rendered tables: the same substrate pinned into human-readable form, independent of either engine.
- Ledger rows emitted: 8 (full run, no cap).

[Collider]:

| Aspect | al-Mirtāl (R1) | Isnād Studio (R2) | ARABIC_TIMELESS (R3) |
|---|---|---|---|
| Detection surface | Weighted relational tables over the recitation array | Contour echoes verified across distant regions | Era-independent structural rules |
| Structural behavior | Convergence by relation: the same pairs keep reappearing | Convergence by contour: the same shapes keep resurfacing | Convergence by contract: the same rules hold in any era |
| Limit / friction | Convergence is the one phenomenon none of the three architectures can produce alone — it exists only in the comparison. Each stack is independent by design, yet their footprints match; the match itself is the finding |
