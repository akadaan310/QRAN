# dependencies
node_modules/
# builds
dist/
build/
.next/
# local env (never commit secrets)
.env
.env.local
# OS / editor
.DS_Store
Thumbs.db
*.swp
# prototype PWA artifacts regenerated by build
prototype/dist/
# generated copy of the repo-root data/ layer for runtime fetch — regenerate
# with `npm run predev` / `npm run prebuild` (scripts/sync-data.mjs); the
# repo-root data/ is the single source of truth, never this copy
prototype/public/data/
